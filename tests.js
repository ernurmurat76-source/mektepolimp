const fs=require('fs'),assert=require('assert'),db=JSON.parse(fs.readFileSync(__dirname+'/data/db.json','utf8'));assert.equal(db.settings.title,'MektepOlimp');assert.deepEqual([...new Set(db.questions.map(q=>q.id))].length,db.questions.length);for(const q of db.questions){assert.ok(q.options.length>=2);assert.ok(q.correct>=0&&q.correct<q.options.length)}console.log('OK: деректер құрылымы тексерілді');
